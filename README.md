# User-management-system-
To create a web application that retrieves, displays, and updates user data from a MySQL database by implementing CRUD operations (Create, Read, Update, Delete) using PHP &amp; MySQL, with proper input validation and secure data storage.
